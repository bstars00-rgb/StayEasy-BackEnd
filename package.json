{
  "name": "stayeasy-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test",
    "smoke": "node scripts/smoke-test.js",
    "db:init": "node scripts/init-normalized-schema.js"
  },
  "dependencies": {
    "pg": "^8.21.0"
  },
  "engines": {
    "node": ">=20"
  }
}
